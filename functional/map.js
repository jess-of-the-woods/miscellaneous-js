var oldArray = [1,2,3,4,5];
var newArray = oldArray.map(function(val){
  return val + 3;
});

/*
This map function adds 3 to every value in the variable oldArray, and saves the results into variable newArray.
*/